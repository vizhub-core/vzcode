import React from 'react';
import ReactDOM from 'react-dom/client'; // Note: Use /client for React 18+

const root = ReactDOM.createRoot(
  document.getElementById('root'),
);
root.render(<h1>Hello, World!</h1>);
